# This is a Databricks asset bundle definition for config_repo.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: config_repo

include:
  - resources/*.yml
  - variables.yml

